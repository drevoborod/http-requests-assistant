+ 1. Add timestamps to log.
+ 2. Make a decision: should we declare parameter type as optional or mandatory? - Decision: type remains optional.
3. Make substitution of values recursive instead of converting to string and back.
4. Write tests of substitution functions (in "core" module).